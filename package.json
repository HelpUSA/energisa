{
  "name": "energisa",
  "version": "2.0.0",
  "private": true,
  "description": "HelpUS Energisa Analytics dashboard with PostgreSQL/Railway persistence.",
  "scripts": {
    "start": "python -m http.server 5173",
    "check": "node --check app.js && node --check api/health.js && node --check api/summary.js && node --check api/imports.js && node --check db/client.js",
    "dev": "vercel dev",
    "build": "echo Static app - no build step"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
